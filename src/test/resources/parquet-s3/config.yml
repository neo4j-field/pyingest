server_uri: ${boltURI}
admin_user: neo4j
admin_pass: neo4j
files:
  - url: s3://pyingest-parquet-test/small/
    type: parquet
    parquet_as_dataset: True
    cql: |
      WITH $dict.rows as rows UNWIND rows as row
          MERGE (a:ALabel {p1: row.p1, p2: row.p2, p3: row.p3})